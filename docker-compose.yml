version: '3.8'

services:
  inayat-app:
    build: .
    container_name: inayat_agent
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: always
